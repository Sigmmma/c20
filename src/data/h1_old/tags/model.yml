name: model
id: mode
invaderStructName: Model
comments:
  en: Stores the render model of [objects][object] in the original Xbox version of the game.
  fields:
    - name: flags
      fields:
        - name: blend shared normals
          en: >
            On map compilation, vertices with the same positions have their normals
            linearly averaged.
